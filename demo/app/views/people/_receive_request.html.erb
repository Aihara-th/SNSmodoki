<%= form_for(current_person.relationshipsA.find_by(personB_id: @user.id),
             html: { method: :delete }) do |f| %>
<%= f.submit "OK", class: "btn1" %>
<%= f.submit "SlightlyOK", class: "btn1" %>
<%= f.submit "Rejection", class: "btn1" %>
<% end %>
